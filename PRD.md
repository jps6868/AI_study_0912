# 대리점 매출 대시보드 PRD

## 1. 목적
서울 시내 대리점의 매출 현황을 한눈에 파악할 수 있는 웹 대시보드를 만든다. Supabase에 저장된 가상 매출 데이터를 시각화하여 대리점별/품목별/기간별 매출 추이를 확인한다.

## 2. 데이터 소스 (Supabase)
- `dealerships` (대리점 10개): id, name, district, address, opened_at
- `items` (품목 10개, 대리점당 1개): id, dealership_id, name, category, unit_price
- `sales` (2026-01-01 ~ 2026-09-12 일별 매출): id, item_id, sale_date, quantity, amount

프론트엔드에서 Supabase JS 클라이언트로 anon key를 통해 직접 조회 (RLS 읽기 정책 필요).

## 3. 핵심 화면 구성

### 3.1 상단 KPI 카드
- 총 매출액 (기간 합계)
- 총 판매 수량
- 활성 대리점 수 / 품목 수
- 전기간 대비 증감률 (선택 기간 vs 직전 동일 기간)

### 3.2 매출 추이 차트
- 일별/주별/월별 매출 라인 차트 (기간 필터 연동)

### 3.3 대리점별 매출 랭킹
- 대리점별 매출 합계 바 차트 (상위 N개, 구 단위 그룹핑 가능)

### 3.4 카테고리(품목)별 매출 비중
- 카테고리별 매출 비중 파이/도넛 차트 또는 바 차트

### 3.5 상세 테이블
- 대리점/품목/기간 조건으로 필터링 가능한 매출 상세 테이블 (정렬, 페이지네이션)

## 4. 필터 / 인터랙션
- 기간 선택 (날짜 범위, 프리셋: 최근 7일/30일/전체)
- 대리점(구) 선택
- 카테고리 선택
- 필터 변경 시 KPI/차트/테이블 동시 갱신

## 5. 기술 스택
- Vite + React (TypeScript)
- Supabase JS Client (데이터 조회)
- Recharts (차트)
- Tailwind CSS (스타일링)
- Vercel (배포) — 환경변수로 Supabase URL/Anon Key 주입

## 6. 비기능 요구사항
- 반응형 레이아웃 (데스크톱 우선, 태블릿까지 고려)
- 초기 로딩 3초 이내 (샘플 데이터 규모 기준)
- 다크/라이트 모드는 범위 밖 (필요 시 추후 확장)

## 7. 범위 제외 (Out of Scope)
- 사용자 인증/권한 관리
- 데이터 입력/수정 UI (CRUD는 SQL로만 관리)
- 실시간(realtime) 업데이트
- 다국어 지원

## 8. 성공 기준
- 필터를 조합해 특정 대리점·기간·카테고리의 매출을 3클릭 이내로 확인 가능
- Vercel에 배포된 URL에서 정상 동작
